<nav class="navbar fixed-top navbar-dark bg-dark">
    <% if (locals.back !== false) { %>
        <div class="col-2">
            <a href="#" class="navbar-brand navsearch" onclick="history.go(-1)">‹ Back</a>
        </div>
        <div class="col-8">
            <input class="form-control" type="search" placeholder="Search items or outfits e.g. casual outfit" aria-label="Search">
        </div>
        <% } else { %>
            <div class="col-10">
                <form action="/search_result">
                    <div class="input-group">
                        <input class="form-control" type="search" placeholder="Search items or outfits e.g. casual outfit" aria-label="Search" value="<%= locals.fill ? locals.fill : '' %>">
                        <div class="input-group-append">
                            <button class="btn btn-light" type="submit">
                                <i class="fas fa-search"></i>
                            </button>
                        </div>
                    </div>
                </form>
            </div>
            <% } %>
                <div class="col-2">
                    <a href="/pickup_list" class="navbar-brand navsearch">
                        <i class="fas fa-list" aria-label="2 items in your pick up list"></i>
                        <span class="badge badge-success">2</span>
                    </a>
                </div>
</nav>